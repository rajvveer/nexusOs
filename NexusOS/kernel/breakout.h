/* ============================================================================
 * NexusOS — Breakout (Header) — Phase 42
 * ============================================================================
 * Classic brick-breaker on the NexusSDL gaming framework — the second game
 * client, proving the API is reusable beyond the raycaster.
 * ============================================================================ */

#ifndef BREAKOUT_H
#define BREAKOUT_H

/* Run the game fullscreen (blocking); prints a summary on exit. */
void breakout_run(void);

#endif /* BREAKOUT_H */
