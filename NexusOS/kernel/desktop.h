/* ============================================================================
 * NexusOS — Desktop Compositor (Header)
 * ============================================================================
 * Main GUI event loop. Composites desktop background, windows, taskbar,
 * and mouse cursor into a single frame.
 * ============================================================================ */

#ifndef DESKTOP_H
#define DESKTOP_H

/* Enter the desktop GUI mode (main event loop, returns when user exits) */
void desktop_run(void);

#endif /* DESKTOP_H */
