/* ============================================================================
 * NexusOS — Lock Screen (Header)
 * ============================================================================ */

#ifndef LOCKSCREEN_H
#define LOCKSCREEN_H

/* Run the lock screen (blocks until correct password entered) */
void lockscreen_run(void);

#endif /* LOCKSCREEN_H */
